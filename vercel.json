{
  "buildCommand": "npm run build -w auronix-frontend && rm -rf dist && cp -r frontend/dist dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
