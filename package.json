{
  "name": "auronix",
  "version": "1.0.0",
  "private": true,
  "description": "Auronix Technologies — Cybersecurity Services & Client Portal",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n FE,BE -c cyan,green \"npm run dev -w auronix-frontend\" \"npm run dev -w auronix-backend\"",
    "dev:frontend": "npm run dev -w auronix-frontend",
    "dev:backend": "npm run dev -w auronix-backend",
    "build": "npm run build -w auronix-frontend",
    "lint": "npm run lint -w auronix-frontend",
    "db:migrate": "npm run db:migrate -w auronix-backend",
    "db:seed": "npm run db:seed -w auronix-backend",
    "db:studio": "npx prisma studio --schema=backend/prisma/schema.prisma"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "engines": {
    "node": "20.x"
  }
}
